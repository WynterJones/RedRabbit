generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model communities {
  id   Int     @default(autoincrement()) @id
  name String? @unique
}

model posts {
  attached_image  String?
  community_id    Int?
  created_at      DateTime?
  id              Int       @default(autoincrement()) @id
  included_image  String?
  included_link   String?
  sentiment_score Float?
  snippet         String?
  title           String?
  url             String?   @unique
}